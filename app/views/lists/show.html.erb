<h1><%= @list.name %></h1>

<h2>Films dans cette liste :</h2>
<ul>
  <% @list.bookmarks.each do |bookmark| %>
    <li>
      <strong><%= bookmark.movie.title %></strong> - <%= bookmark.comment %>
      <%= link_to "Supprimer", bookmark_path(bookmark), method: :delete, data: { confirm: "Êtes-vous sûr ?" }, class: "btn btn-danger btn-sm" %>
    </li>
  <% end %>
</ul>

<%= link_to "Ajouter un film", new_list_bookmark_path(@list), class: "btn btn-success" %>
